<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulaire Agricuteur</title>
    <style>
        /* Style global */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #eaf4e6; /* Fond clair vert pâle */
            margin: 0;
            padding: 0;
            color: #3e5b41; /* Couleur du texte verte */
        }

        /* Conteneur central du formulaire */
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            padding: 20px;
        }

        /* Style pour le formulaire avec thème "agriculteur" */
        .agriculteur-form {
            background-color:rgb(239, 244, 239); /* Couleur de fond verte très claire */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 600px; /* Limite la largeur du formulaire */
        }

        /* Style pour les champs du formulaire */
        .agriculteur-form input,
        .agriculteur-form select,
        .agriculteur-form textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 2px solid #3e5b41; /* Bordure verte, comme les champs agricoles */
            border-radius: 4px;
            background-color:rgb(247, 245, 245); /* Fond vert clair pour les champs */
            font-size: 16px;
        }

        /* Style pour les labels */
        .agriculteur-form label {
            font-weight: bold;
            color: #2d5a2a; /* Vert foncé pour les labels */
            margin-bottom: 5px;
            display: block;
        }

        /* Style pour les boutons */
        .agriculteur-btn {
            background-color: #3e5b41; /* Vert foncé */
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
        }

        .agriculteur-btn:hover {
            background-color: #2d5a2a; /* Couleur verte plus foncée pour le survol */
        }

        /* Style pour les erreurs */
        .agriculteur-form .form-error {
            color: red;
            font-size: 12px;
            margin-top: 5px;
        }
    </style>
</head>
<body>

<div class="container">
    {{ form_start(form, {'attr': {'class': 'agriculteur-form'}}) }}
        {{ form_widget(form) }}
        <button class="btn agriculteur-btn">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }}
</div>

</body>
</html>
