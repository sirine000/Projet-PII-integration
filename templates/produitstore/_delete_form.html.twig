<form method="post" action="{{ path('app_produitstore_delete', {'id': produit_store.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit_store.id) }}">
    <button class="btn">Delete</button>
</form>
